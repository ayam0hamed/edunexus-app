# TODO

- [ ] Create an edit plan for making `login_screen.dart` a static login page.
- [x] Update `LoginScreen` UI to remove Bloc/Cubit dependencies and make it fully static.
- [x] Add simple text fields/controllers and a static LOGIN NOW button with local validation + snackbar.

- [ ] Ensure the file compiles by removing unused imports and fixing any missing variables currently referenced (e.g., `_formKey`, `_emailController`, `_passwordController`, `_rememberMe`).
- [ ] Run `flutter analyze` (and optionally `flutter test`) to verify no compile errors.

